{{template "base" .}}
{{define "title"}}Home{{end}}
{{define "main"}}
    <h2>Latest News</h2>
    {{if .NewsArray}}
        <table>
            <tr>
                <th>Title</th>
                <th>Created</th>
                <th>ID</th>
            </tr>
            {{range .NewsArray}}
                <tr>
                    <td><a href='/news/{{.ID}}'>{{.Title.String}}</a></td>
                    <td>{{.Created | humanDate}}</td>
                    <td>#{{.ID}}</td>
                </tr>
            {{end}}
        </table>
    {{else}}
        <p>There's nothing to see here... yet!</p>
    {{end}}
{{end}}